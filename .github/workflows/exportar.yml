name: Exportar Datos

on:
  workflow_dispatch:  # ejecución manual
  schedule:
    - cron: "0 7 * * *"  # todos los días a las 7am UTC

jobs:
  exportar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: |
          pip install playwright
          playwright install chromium

      - name: Ejecutar script
        env:
          USUARIO_PORTAL: ${{ secrets.USUARIO_PORTAL }}
          PASS_PORTAL: ${{ secrets.PASS_PORTAL }}
        run: python TOATCH1609.py

      - name: Enviar archivo por email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: "Reporte automático de datos exportados"
          to: selfgeneratedcamillogonzalez@gmail.com
          from: "Exportador BOT <${{ secrets.EMAIL_USER }}>"
          body: |
            Hola Camilo 👋

            Te envío en adjunto el reporte exportado automáticamente desde el portal.

            Saludos,
            BOT Exportador
          attachments: exportado.xlsx
